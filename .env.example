# ABSTRAL Pipeline — Required API Keys
# Copy this to .env and fill in your keys.

# Meta-agent (BUILD, ANALYZE, UPDATE): Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Agent backbone (RUN): OpenAI
OPENAI_API_KEY=sk-proj-...

# OpenTelemetry trace export (optional)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# HuggingFace (GAIA dataset access — gated dataset, requires approval)
HF_TOKEN=hf_...
